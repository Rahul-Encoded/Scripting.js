let testScore = 92;
let letterGrade;

switch(true){
    case testScore >= 90:
        letterGrade = "A"
        break
    case testScore >= 80:
        letterGrade = "A"
        break
    case testScore >= 70:
        letterGrade = "A"
        break
    case testScore >= 60:
        letterGrade = "A"
        break
    default:
        letterGrade = "F"
}

console.log(`Your Grade is ${letterGrade}`)